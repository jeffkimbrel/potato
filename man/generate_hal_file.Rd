% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kofam.R
\name{generate_hal_file}
\alias{generate_hal_file}
\title{Generate HAL file for KOfam annotation}
\usage{
generate_hal_file(
  ko_terms,
  config,
  output_dir = "results/kofam/",
  overwrite = FALSE
)
}
\arguments{
\item{ko_terms}{Character vector of KO IDs (e.g., c("K00001", "K00002"))}

\item{config}{Potato config object from load_potato_config()}

\item{output_dir}{Directory to write .hal file (default: "results/kofam/")}

\item{overwrite}{Logical. If FALSE, reuses existing .hal file with same hash}
}
\value{
Path to the generated .hal file
}
\description{
Creates a .hal file listing HMM profile paths for specified KO terms.
HAL files are named by the hash of their KO content, allowing reuse across
multiple genomes and potatoes with the same KO requirements.
}
\details{
The .hal file format is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{/path/to/profiles/K00001.hmm
/path/to/profiles/K00002.hmm
}\if{html}{\out{</div>}}

File is named: \verb{\{hash\}.hal} where hash is MD5 of sorted KO terms.
}
\examples{
\dontrun{
config <- load_potato_config()
potato <- load_test_potato()
ko_terms <- get_detection_terms(potato, "ko")
hal_file <- generate_hal_file(ko_terms, config)
}
}
\keyword{internal}
