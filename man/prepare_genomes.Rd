% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genomes.R
\name{prepare_genomes}
\alias{prepare_genomes}
\title{Load genome files as jakomics FILE objects}
\usage{
prepare_genomes(genome_dir = NULL, genome_files = NULL, validate = TRUE)
}
\arguments{
\item{genome_dir}{Directory containing genome files}

\item{genome_files}{Character vector of specific genome file paths (alternative to genome_dir)}

\item{validate}{Logical. If TRUE, validates .faa files using BioPython (default: TRUE)}
}
\value{
List of jakomics FILE objects (reticulate Python objects)
}
\description{
Discovers genome files (.faa, .gbk, .gb, .gbff) and returns them as a list of
jakomics FILE objects for downstream processing.
}
\details{
This function discovers genome files and validates .faa files using
\code{jakomics.utilities.validate_fasta()}. Genbank files (.gb, .gbk, .gbff) are
not converted here - use \code{convert_genbank_to_faa()} for that.

The returned FILE objects are reticulate Python objects with properties:
\itemize{
\item \verb{$file_path} - Full path to file
\item \verb{$name} - Filename with extension
\item \verb{$short_name} - Filename without extension
\item \verb{$suffix} - File extension (e.g., ".faa")
\item \verb{$id} - Unique ID (UUID hex)
}
}
\examples{
\dontrun{
# Load all genome files from directory
genomes <- prepare_genomes(genome_dir = "genomes/")

# Load specific files
genomes <- prepare_genomes(genome_files = c("genome1.faa", "genome2.gbk"))

# Access properties
genomes[[1]]$name
genomes[[1]]$file_path
}
}
